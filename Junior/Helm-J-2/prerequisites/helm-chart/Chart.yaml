apiVersion: v2
name: simple-nginx-chart
description: A basic Helm chart for deploying Nginx (Prerequisite for Helm-J-2).
# Application charts should be managed reproducible sources, **not** disposable.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
version: 0.1.1

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application.
appVersion: "1.25-alpine" # Reflect the default app/container version you intend to deploy

# Helm-J-2: added Pod label; bump chart to 0.1.1; align appVersion to 1.25-alpine.